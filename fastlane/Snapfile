# fastlane snapshot config for Lore's App Store screenshots.
#
# Runs on the Simulator (no code signing). Captures the non-camera surfaces for
# both storefront families: the 6.9" iPhone size and the full 13" iPad canvas.
# One device in each family covers the smaller storefront slots.
#
# Regenerate any time: `bundle exec fastlane screenshots`.

# Device names must exist in the Xcode the runner actually has, or snapshot
# aborts before capturing anything. Xcode 26 (macos-15 / local 26.6) ships the
# iPhone 17 family; "iPhone 16 Pro Max" no longer resolves. Re-check against
# `xcrun simctl list devices available` when bumping Xcode.
devices([
  "iPhone 17 Pro Max",
  "iPad Pro 13-inch (M5)",
])

languages([
  "en-US"
])

# The scheme wired for testing in project.yml (its Test action runs LoreUITests).
scheme("LoreScreenshots")

# Where the PNGs land (also the artifact the workflow uploads).
output_directory("./fastlane/screenshots")

# Start clean so a re-run never mixes old and new frames.
clear_previous_screenshots(true)

# One simulator at a time keeps memory low and network loads deterministic.
concurrent_simulators(false)

# Surface a broken capture immediately instead of half a set.
stop_after_first_error(true)

# The app pulls real content over the network; give slow first renders room.
number_of_retries(2)
