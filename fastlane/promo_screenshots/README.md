# Lore 1.2 screenshots

These twelve original native captures come from release source `261d633d277a9940b849b108df0f10776e657f76`, recorded in `SOURCE_SHA`. They were captured on September 5, 2026 after content revision `2026090501` finished publishing, using the existing Snapfile and screenshot test. No image pixels were edited or regenerated.

There are six iPhone 17 Pro Max frames at 1320 × 2868 and six iPad Pro 13-inch (M5) frames at 2064 × 2752. All are opaque RGB PNGs. `CAPTURE.json` records device/tool versions, the original filenames, dimensions, and SHA-256 hashes.

The six surfaces are Map, Tours, Willis Tower Dive, Meet Chicago, Passport, and Profile. All twelve were visually inspected for rendered content, system dialogs, loading placeholders, and layout artifacts. The iPad sidebar has no blank floating tab capsule. Passport and Profile show the authentic signed-out experience and unearned milestones; they do not show invented private records. The native iPad sheets and window resize affordance remain as captured.

Capture uses a Debug build with only the existing navigation/onboarding screenshot hooks and standard simulator status-bar settings. Membership is not artificially unlocked. The release binary and this package share the recorded app source; the screenshot package commit contains no application changes.
