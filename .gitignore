*.xcodeproj/
xcuserdata/
.DS_Store
build/
DerivedData/
__pycache__/
*.py[cod]

# fastlane generated
fastlane/report.xml
fastlane/README.md
fastlane/test_output/
# Raw snapshot output. Regenerated by `fastlane screenshots` and uploaded as a
# CI artifact; the curated frames that ship to the App Store are the tracked
# copies in fastlane/promo_screenshots/.
fastlane/screenshots/
*.ipa
*.dSYM.zip

# Fastlane cloud-signing temp key (written from secret at CI time)
fastlane/AuthKey.p8
fastlane/*.p8
**/*.p8

# Narration pipeline: local venv, model weights, rendered audio (uploads go to
# Supabase Storage, never the repo)
tools/narration/.venv/
tools/narration/models/
tools/narration/out/
